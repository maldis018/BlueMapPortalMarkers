version: 2
updates:
  # Keep the SHA-pinned GitHub Actions current (raises PRs with updated SHAs
  # and version comments). Grouped so all action bumps land in one PR.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    groups:
      github-actions:
        patterns: ["*"]
